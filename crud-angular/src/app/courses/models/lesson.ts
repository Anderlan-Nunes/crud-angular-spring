export interface Lesson {
  id: string;
  name: string;
  youtubeUrl: string

}

// desde que eu mantenha essa mesma nomeclatura que nós temos no json nao precisamos mecher em nada no resolver pq o resolver ja tem o curso e a lesson esta dentro do curso
